source "https://rubygems.org"

# GitHub Pages 使用固定版本的 Jekyll 及插件来构建站点，
# 用这个 gem 而不是直接依赖 jekyll，可以保证本地构建结果和线上一致。
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

# 兼容较新 Ruby 版本缺少的默认 gem
gem "webrick"
